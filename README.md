# multi-domain-lora-router
A memory-efficient multi-domain LLM system using one shared GPT-2 model, LoRA adapters for code, medical, and general tasks, and a lightweight text router that selects the best adapter for each prompt.
